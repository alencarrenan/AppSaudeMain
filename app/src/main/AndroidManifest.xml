<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appsaudemain">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSaudeMain">

        <!-- Tela principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de opções -->
        <activity
            android:name=".menuOpcoes"
            android:exported="false" />

        <!-- Tela de cadastro -->
        <activity
            android:name=".telaRegistrar"
            android:exported="false" />


        <activity android:name=".MeusRemedios" android:exported="false"/>
        <activity android:name=".MinhasDoencas" android:exported="false"/>



    </application>

</manifest>
